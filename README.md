# ci-practice

A practice repo to build the CI

## Quick start

Install dependencies:

```bash
npm install
```

Run locally:

```bash
npm start
```

Run tests:

```bash
npm test
```

Lint and format:

```bash
npm run lint
npm run format
```
